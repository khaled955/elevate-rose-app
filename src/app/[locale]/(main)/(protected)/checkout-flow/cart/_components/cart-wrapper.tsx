import CartHeader from "./cart-header";
import CartList from "./cart-list";

export default function CartWrapper() {
  return (
    <section>
      <CartHeader />
      <CartList/>
       
    </section>
  );
}
